# 验收日志：T1 环境搭建与基础设施（部分完成）

- 任务范围：`.kiro/specs/second-brain-os/tasks.md` 第 1 章（环境搭建和基础设施）
- 日期：2026-03-20

## 本次变更摘要

- `backend/sbo_core/config.py`
  - 增加 WeKnora（Episodic）配置项：`WEKNORA_ENABLE/WEKNORA_BASE_URL/WEKNORA_API_KEY/WEKNORA_REQUEST_TIMEOUT_MS/WEKNORA_RETRIEVAL_TOP_K/WEKNORA_RETRIEVAL_THRESHOLD/WEKNORA_TIME_DECAY_RATE/WEKNORA_SEMANTIC_WEIGHT/WEKNORA_TIME_WEIGHT`
  - 当 `WEKNORA_ENABLE=true` 时，缺失 WeKnora 必需配置会在启动时明确报错
- `backend/.env.example`
  - 补齐 WeKnora 最低环境变量集与示例默认值
- `backend/pyproject.toml`
  - 补齐运行依赖：`python-dotenv`（现有 smoke scripts 已使用）
- `backend/scripts/weknora_smoke_test.py`
  - WeKnora 连通性与鉴权验证脚本（携带 `X-Request-ID`）
- `backend/scripts/weknora_kb_smoke_test.py`
  - WeKnora KnowledgeBase 管理最小闭环验证脚本（列出 -> 创建 -> 再列出）
- `backend/tests/test_settings.py`
  - 新增单元测试：`WEKNORA_ENABLE=true` 且缺失必需配置时必须失败
- `.gitignore`
  - 允许提交 `reports/*.md`（按仓库规则，验收日志必须落盘并纳入版本管理）
- `.kiro/specs/second-brain-os/tasks.md`
  - 已勾选完成：1.1 / 1.1.1 / 1.2 / 1.3

## 自动化验证

### 1) 基础自动化校验（compileall）

- 命令：
  - `./.venv/bin/python -m compileall backend`
- 结果：PASS

### 2) 单元测试（pytest）

- 命令：
  - `./.venv/bin/python -m pytest -q backend`
- 结果：PASS

## 冒烟测试（真实服务）

本次已执行（要求本机已启动真实服务，并在 `backend/.env` 中配置连接信息）。

执行的脚本（全部要求 PASS，缺失配置必须失败）：

- `backend/scripts/postgres_smoke_test.py`（Postgres + pgvector）
- `backend/scripts/redis_smoke_test.py`（Redis + RQ enqueue）
- `backend/scripts/weknora_smoke_test.py`（WeKnora 连通性 + API Key + `X-Request-ID`）
- `backend/scripts/weknora_kb_smoke_test.py`（WeKnora KnowledgeBase 最小闭环）

结果：

- Postgres：PASS
- Redis：PASS
- WeKnora 连通性与鉴权：PASS
- WeKnora KnowledgeBase 最小闭环：PASS

## 零遗留项声明（当前状态）

- 结论：满足（第 1 章“环境搭建和基础设施”任务已全部完成，且自动化验证均已通过）
- 未完成的任务：无

1.6 验证结果：

- 依赖安装：`npm install`（在 `web/` 目录）
- 前端构建命令：`npm run build`（在 `web/` 目录）
- 结果：PASS
- 安全扫描（npm audit）：PASS（通过在 `web/package.json` 增加 `overrides` 强制升级 `esbuild` 至修复版本，避免引入 `vite@8` 的 breaking change）

- 审查问题：无（本次未生成审查报告；也未发现阻断性问题）

## 备注

- 说明：本日志已覆盖“基础校验 + 单元测试 + 真实服务冒烟（Postgres/Redis/WeKnora）+ 前端构建基线”。
