# SecondBrainOS

本仓库是 SecondBrainOS 的代码与文档根目录。

## 目录结构

- `backend/`：SecondBrainOS Core（FastAPI + RQ worker）
- `web/`：独立 Web 前端（Vite + React + TypeScript）
- `docs/`：文档体系
- `reports/`：验收日志与审查报告
- `zip/`：前端原型（参考实现，不作为正式发布入口）
